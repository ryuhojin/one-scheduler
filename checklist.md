# one-line 개발 체크리스트

이 문서는 순차 개발 기준 문서다. 항상 위에서 아래 순서로 진행한다.

## 0. 프로젝트 킥오프
- [ ] 프로젝트 목표/범위 확정(사용자 앱 + 관리자 웹 + API)
- [ ] 일정 분류 규칙 확정(회사/부서/그룹/개인)
- [ ] 성공 지표 정의(응답속도, 실시간 지연, 알림 성공률)
- [ ] 개발/운영 환경(Dev, Staging, Prod) 분리 정책 확정
- [x] 개발 표준 확정: 백엔드/웹은 Docker Compose 기반 로컬 개발

검증
- [ ] 요구사항 문서와 `AGENTS.md` 충돌 없음

## 1. 모노레포 및 기본 인프라 세팅
- [x] 모노레포 초기화(`apps`, `packages`)
- [x] `apps/electron-user` 생성
- [x] `apps/admin-web` 생성
- [x] `apps/api` 생성
- [x] `packages/shared` 생성(공통 타입, 권한 상수)
- [ ] 공통 lint/format/test 스크립트 구성
- [x] 환경변수 템플릿(`.env.example`) 작성
- [x] `docker-compose.yml` 작성(`api`, `admin-web`, `postgres`, `redis`)
- [x] `apps/api/Dockerfile.dev` 작성(핫리로드)
- [x] `apps/admin-web/Dockerfile.dev` 작성(Vite dev 서버)
- [x] 볼륨/포트/헬스체크 및 의존성 순서 설정

검증
- [x] 전체 앱 빌드/실행 스모크 테스트 통과
- [x] `docker compose config` 검증 통과
- [ ] `docker compose up` 시 API/웹/DB/Redis 정상 기동

## 2. 데이터 모델링/DB 설계
- [ ] 테이블 설계: Company, Department, User, Group, GroupMember
- [ ] 테이블 설계: Schedule, ScheduleShare, ScheduleAuditLog
- [ ] 테이블 설계: Notification, DeviceSession, AuthSession
- [ ] 인덱스/유니크/외래키 설계
- [ ] 마이그레이션 + 시드 데이터 작성

검증
- [ ] 마이그레이션 up/down 정상 동작
- [ ] 권한 범위별 조회 쿼리 성능 확인(기본 인덱스 사용)

## 3. 인증/인가(RBAC) 구현
- [ ] 로그인/토큰 재발급/로그아웃 API
- [ ] 역할 기반 접근 제어 미들웨어 구현
- [ ] 테넌트 경계 검증(회사 간 접근 차단)
- [ ] 세션 만료/강제 로그아웃 처리

검증
- [ ] 권한 없는 사용자 접근 차단 테스트
- [ ] 토큰 탈취 가정 시나리오 기본 방어 확인

## 4. 조직/사용자/그룹 관리 API
- [ ] 사용자 CRUD + 초대 플로우
- [ ] 부서 CRUD + 사용자 배정
- [ ] 그룹 생성/수정/삭제
- [ ] 그룹 초대/수락/거절/강퇴

검증
- [ ] 부서/그룹 경계에서 권한 우회 불가 확인

## 5. 일정 API(핵심 기능)
- [ ] 일정 생성 API(회사/부서/그룹/개인)
- [ ] 일정 수정/삭제 API
- [ ] 일정 목록/상세 조회 API
- [ ] 개인일정 선택 공유 API(특정 유저 지정)
- [ ] 반복 일정(선택) 설계 여부 결정 및 반영

검증
- [ ] 생성자/공유자/비공유자 시나리오 권한 테스트
- [ ] 분류별 조회 범위 정확성 테스트

## 6. 실시간 동기화(WebSocket)
- [ ] 인증된 소켓 연결
- [ ] 회사/부서/그룹/개인 공유 범위별 룸 설계
- [ ] 일정 생성/수정/삭제 이벤트 발행
- [ ] 오프라인 복귀 시 누락 동기화 로직

검증
- [ ] 다중 사용자 동시 접속 시 이벤트 누락 없음
- [ ] 권한 없는 이벤트 수신 없음

## 7. PC 알림(Electron)
- [ ] 신규 공유 일정 수신 시 OS 알림 표시
- [ ] 알림 클릭 시 해당 일정 상세로 이동
- [ ] 중복 알림 방지 키 적용
- [ ] 알림 허용/차단 사용자 설정 제공

검증
- [ ] 실시간 이벤트와 알림 표시가 1회씩 정확히 동작

## 8. 사용자 앱 UI 구현(Electron)
- [ ] 캘린더 뷰(월/주/일)
- [ ] 일정 등록/수정/삭제 UI
- [ ] 분류별 필터(회사/부서/그룹/개인)
- [ ] 공유 대상 선택 UI(개인일정 전용)
- [ ] 그룹 관리 UI(생성/멤버 초대)

검증
- [ ] 핵심 시나리오 수동 테스트 체크리스트 통과

## 9. 관리자 웹 UI 구현(Admin)
- [ ] 로그인/세션 관리 UI
- [ ] 사용자/부서/그룹 관리 UI
- [ ] 권한 정책 관리 UI
- [ ] 감사로그 조회 UI
- [ ] 통계 대시보드 UI
- [ ] 공지/긴급 배너 관리 UI

검증
- [ ] 관리자 기능별 권한 분리 확인(ADMIN vs SUPER_ADMIN)

## 10. 감사로그/모니터링/운영
- [ ] 일정 변경 감사로그 저장 및 검색 API
- [ ] 에러 로깅/메트릭 수집
- [ ] 알림 실패 재시도 큐
- [ ] 운영 대시보드 기본 알림(오류율/지연)

검증
- [ ] 주요 이벤트 추적 가능 여부 확인

## 11. 테스트 고도화
- [ ] 단위 테스트(도메인 로직, 권한 검증)
- [ ] 통합 테스트(API + DB)
- [ ] E2E 테스트(사용자 앱 핵심 플로우)
- [ ] 부하 테스트(실시간 이벤트)

검증
- [ ] CI에서 테스트 자동 실행/통과

## 12. 보안 점검
- [ ] 민감정보 마스킹 점검
- [ ] 입력 검증/SQL Injection/XSS 기본 점검
- [ ] Rate Limit/Brute-force 대응
- [ ] 보안 헤더/CORS 정책 검토

검증
- [ ] 보안 체크리스트 통과 보고서 작성

## 13. 배포/릴리스
- [ ] API 배포 파이프라인 구성
- [ ] Admin 웹 배포 파이프라인 구성
- [ ] Electron 빌드/배포(업데이트 전략 포함)
- [ ] 롤백 전략/런북 작성

검증
- [ ] Staging 종단간(E2E) 테스트 통과

## 14. 운영 전환
- [ ] 운영 계정/권한 정비
- [ ] 초기 사용자 온보딩 가이드 배포
- [ ] 장애 대응 연락 체계 확정
- [ ] 1차 운영 모니터링 지표 리뷰

검증
- [ ] Go-Live 체크리스트 승인

---

## 단계 진행 규칙
- [ ] 한 단계 완료 전 다음 단계 착수 금지
- [ ] 단계 완료 시 변경사항/검증 결과/리스크를 기록
- [ ] 큰 변경(의존성, DB 대규모 변경, 인증/권한 정책, 로깅 정책)은 사전 합의 후 진행
